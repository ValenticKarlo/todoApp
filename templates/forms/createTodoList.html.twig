{% extends 'base.html.twig' %}
{% block title %}Create list{% endblock %}

{% block body %}
    <div class="container">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class=" btn btn-sm btn-secondary position-static d-grid gap-1  rounded">Submit</button>
        {{ form_end(form) }}
    </div>
{% endblock %}